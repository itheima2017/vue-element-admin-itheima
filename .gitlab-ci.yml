stages:
  - test
  - build
  - deploy

sonar_test:
  stage: test
  script:
    - ci/sonar.sh
  tags:
    - hmadmin-vue

build_image:
  stage: build
  # services:
  #   - node:8
  script: 
    - ci/build.sh
  tags:
    - hmadmin-vue

deploy_image:
  stage: deploy
  # services:
  #   - node:8
  script: 
    - ci/deploy.sh
  tags:
    - hmadmin-vue